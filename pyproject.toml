[tool.ruff]
line-length = 110
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B"]
ignore = ["E501"]   # long lines tolerated in templates/strings

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["."]
